{{> header}}
<div class="col-md-6 text-center col-lg">
    <img class="profile-img" src="./images/user.png" />
    <div class="profile-info">
        <p>Username: <small>{{username}}</small></p>
        <p class="infoType">Organizer of {{numberOfEvents}} events.</p>
        {{#each events}}

        {{this}}
        {{else}}
        <p>No events</p>
        {{/each}}
        <p>No events</p>
    </div>
</div>

{{> footer}}